# Docker profile — overrides only what changes when running inside Docker.
# Everything else inherits from application.yml.

spring:
  kafka:
    # Inside Docker, app connects via internal listener (service name)
    # application.yml has localhost:29092 (for running on host machine)
    bootstrap-servers: kafka:9092

    listener:
      auto-startup: true   # explicitly ensure consumers start in Docker